function switchLocation() //This function will switch the users location back to MissionControl.html when called
{
    location.replace("../MissionControl/MissionControl.html");
}

function returnToLogin()
{
    //This function will switch the user to the index.html when called
    location.replace("../index.html");
}